import{useCallback,useMemo,useSyncExternalStore}from"react";
export function useSessionProjection(binding,key){const face=useMemo(()=>binding?.session?.projections?.faceOf?.(key)||null,[binding,key]);return useSyncExternalStore(useCallback(listener=>{const off=face?.subscribe?.(listener);return typeof off==="function"?off:()=>{}},[face]),useCallback(()=>face?.getSnapshot?.(),[face]),()=>undefined)}
export function deriveContextExperience(pressure,usage,breakdown){const used=pressure?.projectedTokens??pressure?.pressureTokens,windowSize=pressure?.contextWindow,percent=Number.isFinite(used)&&Number.isFinite(windowSize)&&windowSize>0?Math.min(100,Math.max(0,Math.round(used/windowSize*100))):null,input=(usage?.uncachedInputTokens||0)+(usage?.cacheReadTokens||0)+(usage?.cacheWriteTokens||0),output=usage?.outputTokens||0,total=input+output;return{percent,usedTokens:Number.isFinite(used)?used:null,contextWindow:Number.isFinite(windowSize)?windowSize:null,inputTokens:input,outputTokens:output,totalTokens:total,breakdown:breakdown||null}}
export function formatTokenCount(value){if(!Number.isFinite(value))return"—";if(value>=1e6)return`${(value/1e6).toFixed(value>=1e7?0:1)}m`;if(value>=1e3)return`${(value/1e3).toFixed(value>=1e4?0:1)}k`;return String(Math.round(value))}
