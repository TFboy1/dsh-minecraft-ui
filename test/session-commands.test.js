import test from"node:test";import assert from"node:assert/strict";import{executeSessionCommand,sessionSuggestions}from"../client/src/dsh/session-commands.js";
const sessions=[{id:"s1",displayTitle:"Alpha"},{id:"s2",displayTitle:"Beta",running:true}],opened=[];const services={sessions:{open:id=>opened.push(id)},workspaces:{connectWorkspace:async id=>`new-${id}`}};
test("session command lists and switches by index title and id",async()=>{let r=await executeSessionCommand("/session",{sessions,workspaces:[],services});assert.equal(r.lines.length,2);r=await executeSessionCommand("/session 2",{sessions,workspaces:[],services});assert.equal(r.selected,"s2");r=await executeSessionCommand("/session Alpha",{sessions,workspaces:[],services});assert.equal(r.selected,"s1");r=await executeSessionCommand("/session s2",{sessions,workspaces:[],services});assert.equal(r.selected,"s2");});
test("session new connects workspace and opens result",async()=>{const r=await executeSessionCommand("/session new Demo",{sessions,workspaces:[{id:"w1",title:"Demo"}],services});assert.equal(r.selected,"new-w1");assert.equal(opened.at(-1),"new-w1");});
test("session suggestions filter current input",()=>{const s=sessionSuggestions("/session be",sessions);assert.equal(s.length,1);assert.equal(s[0].id,"s2");});
