import test from"node:test";import assert from"node:assert/strict";import{assertModelCoverage,modelDefinition}from"../client/src/models/registry.js";import{ITEM_DEFS}from"../client/src/inventory.js";
test("every item has one model definition",()=>assert.equal(assertModelCoverage(Object.keys(ITEM_DEFS)),true));test("crafted workstation is not planks",()=>{assert.equal(modelDefinition("crafting_table").kind,"crafting_table");assert.notEqual(modelDefinition("crafting_table").texture,"planks")});test("semantic facilities have distinct models",()=>{assert.equal(modelDefinition("model_chest").semantic,"models");assert.equal(modelDefinition("plugin_chest").semantic,"capabilities");assert.equal(modelDefinition("enchant_table").semantic,"reasoning")});
